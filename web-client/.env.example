# --- Spaceship PRMS web-client (Vite) ---
# Copy to .env.local:  cp web-client/.env.example web-client/.env.local
#
# Empty = same-origin (/api → Vite proxy in dev, nginx in Docker prod).
# Cross-origin API:
# VITE_API_BASE=http://localhost:3001

VITE_API_BASE=
